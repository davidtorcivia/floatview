{
  "name": "floatview",
  "version": "1.2.0",
  "description": "A minimal floating browser window for streaming media on a secondary monitor",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
