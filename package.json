{
  "name": "floatview",
  "version": "0.4.3",
  "description": "A minimal floating browser window for streaming media on a secondary monitor",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
